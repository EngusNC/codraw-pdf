# CoDraw PDF

Application collaborative de dessin sur PDF en temps réel, avec Socket.io.

## Déploiement (Docker + Traefik)

```bash
docker compose up -d --build
```

## Développement local

```bash
npm install
node server.js
# → http://localhost:3000
```
